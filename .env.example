# Copy to .env and adjust (.env is gitignored).
# Bind to all interfaces so phones/tablets on the same Wi?Fi can open http://<this-mac-lan-ip>:PORT/

PRINTIT_SERVE_HOST=0.0.0.0
PRINTIT_SERVE_PORT=8080
